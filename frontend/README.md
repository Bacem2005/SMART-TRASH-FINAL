SmartBin UI (Vite + React + Tailwind)

Quick start:
1) npm install
2) npx tailwindcss -i ./src/index.css -o ./src/output.css --watch  (or use the dev script)
3) npm run dev

Make sure backend API is running at http://localhost:5000 (or set VITE_API_URL)
